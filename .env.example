# Copia este archivo a .env y reemplaza el valor con tu API key de OpenAI
# Asegúrate de que el archivo .env esté listado en tu .gitignore para no subir tu clave secreta.

OPENAI_API_KEY="sk-tu_api_key_aqui"
# Opcionalmente, si usas un modelo específico que no sea el default de OpenAI,
# podrías añadir otras variables de configuración aquí, por ejemplo:
# OPENAI_MODEL_NAME="gpt-4-turbo"
# Por ahora, nos enfocaremos en gpt-4o-mini que es un buen modelo y coste-efectivo.
# Langchain usará gpt-3.5-turbo por defecto si no se especifica un modelo al instanciar ChatOpenAI.
# Para GPT-4.1 Mini, el identificador suele ser "gpt-4o-mini".
OPENAI_MODEL_NAME="gpt-4o-mini"
